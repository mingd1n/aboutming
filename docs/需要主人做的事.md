# 需要主人做的事

> 主人待办日志。Agent **每次**需要你配合（照片、链接、文案、Figma、拍板）时，必须更新本文件：在「现在请做」加一条，并在「日志」写下「这次需要你补充 xxx」。
> 你做完后告诉 Agent，由 Agent 勾掉并挪到「已完成」。
> 文件只认桌面仓库：`~/Desktop/aboutming/docs/需要主人做的事.md`。

## 现在请做

- [ ] **个人摄影** 4–8 张（`minslens_`）→ `materials/photography/`（作品墙，与「镜头 · Guangzhou」城市图分开）
- [ ] **「现在可约什么」** 中英各一句 → `materials/links.md`
- [ ] **音乐 / 电影 / 旅行细节** → `Axel/sections/兴趣/`（歌单、片单、行程；骨架已有）
- [ ] **思考笔记** → 文章/播客等个人思考（不是「我在做」方向笔记；首页 02 已留空位）
- [ ] （可选）**清掉公网可见的残留文字**：`ai-dev.md` 里 `[[学习路线]]` 后的「`<点进去是新的页面>`」、web3 的 `# 作品` 空小节、跨境 `# 项目：` 空小节、自媒体 `# 学习` 的悬空链（AI选题/AI制作/AI视频创作，若打算做就建笔记，不做就删）
- [ ] （可选）五方向同等大小线描 SVG → `materials/marks/`
- [ ] （可选）Figma：`home/01-building` + `tokens` → `materials/figma/`
- [ ] （可选）**动态壁纸素材**：短视频或图序 → `materials/`

不要：网图赛博城、AI 假脸、电话、未授权合影。图请自己拍或自己的账号作品。

> 社交账号已由主人提供并写入 `materials/links.md`（GitHub / Instagram / TikTok / 小红书×2 / 公众号二维码）。X、抖音不在列，已去掉。

## 日志

### 2026-08-24（自媒体账号超链接 + 公众号二维码）

- 主人要求自媒体账号可点击、微信号图片上站。已直接改 vault：`自媒体.md` 的 `# 作品` 从纯文本改成 markdown 链接（Instagram/TikTok/小红书×2），公众号改为嵌入二维码图 `![](/media/QRcode/IMG_5061.JPG)`（站点路径；在 Obsidian 里该图是断链占位，若也想在 Obsidian 显示可自行把二维码放进 vault 再换 wiki 图语法）。
- 站点 `自媒体` 方向页已能点击跳转 + 显示二维码。

### 2026-08-24（方向结构随 vault 重构）

- 主人在 Obsidian 重构了 `我在做：`：方向入口从纯 hub 变成自带 `#学习/#产出` 小节的正文页；删除了 产出/、简历 等文件夹，新增 `作品/`、`项目/`。
- 站点已适配：Tab 改双源推导（入口小节 + 文件夹）、`%20` 链接解码、悬空链显示淡色「未创建」标记。
- **这次需要你补充**：① 是否删掉 ai-dev.md 里会显示到公网的「`<点进去是新的页面>`」提示语；② 空小节（web3 `# 作品`、跨境 `# 项目：`）后续是否填；③ 自媒体 `# 学习` 的三个悬空项目（AI选题/AI制作/AI视频创作）打算建笔记还是删掉。

### 2026-08-20（我的世界：音乐 · 电影 · 旅行）

- 主人要求：My World 含音乐和电影；旅行 ≠ TourGuide。
- vault 已建 `兴趣.md` / 填了 `音乐.md` `电影.md` `旅行.md`（骨架，细节待主人补歌单/片单/行程）。
- 站点：首页 04 三张卡 + `/world`；去掉指向 `/career/tourguide` 的旅行卡。
- **这次需要你补充**：音乐在听/现场、电影看过/想看、旅行细节；摄影墙仍可选。

### 2026-08-20（首页逻辑修复）

- 主人确认按评审改：去掉重复 Building 尾章 → CONNECT；修双滚动与左栏高亮；02 含方向学习；计划诚实空状态；语言切换保路径。
- **这次仍需要你补充**：头像、`photography/` 作品墙、「现在可约什么」；可选清掉 `计划.md` 里指向已删「每日计划」的空链。

### 2026-08-18（vault 结构 + 社交定稿）

- vault 新增 `sections/兴趣/`（音乐 · 电影）；「电影」移出学习思考；「阅读」→「文章」。
- 首页章节改为 4 章（我在做 / 我在学 / 我的计划 / 我的世界），每章内部横向滚轴；「我的知识」并入「我在学」。
- 社交账号以主人最新提供为准，已写入 `materials/links.md`、`README.md`、vault `摄影.md`/`科技文章.md`。公众号二维码已放 `materials/QRcode/IMG_5061.JPG`。X、抖音已去掉。
- **这次需要你补充**：头像、`minslens_` 摄影作品；`links.md` 里「现在可约什么」中英各一句。

### 2026-08-18（首页设计定稿「视觉 OS」）

- 主人提供高保真视觉概念稿，已定稿写入 `AGENTS.md` 与 `docs/项目文档.md` §8.1。
- 首页改为单页滚动，抽象 CSS/SVG 主视觉；城市摄影降级为可选，个人摄影只进 04 章。
- **这次需要你补充**：Figma 可按新帧名画 `home/01-building` + `tokens`。

### 2026-08-18（计划：每日 → 可能每周）

- 主人告知：已删「每日计划」；可能会写「每周计划」并更新到网页 `/plan`。
- 文档已改成不写死每日；上线内容以 `Axel/sections/计划` 为准。
- 备注：`计划.md` 里若仍留着 `[[每日计划]]` 空链，等你加每周计划时自己改一下即可。

### 2026-08-18（双写 + 清理 worktree）

- 主人要求：改计划必须同时改 `AGENTS.md` 与 `docs/项目文档.md`；worktree 跟着桌面同步；已删可扔的不要留。
- 已写入 `AGENTS.md`「双写文档 + worktree 同步」；清理 `first-try-lol`（及检查 `start`）里废弃 Astro 站文件。

### 2026-08-18（技术栈 → Next.js）

- 主人确认：不用 Astro（不好找 page）；要动态壁纸。
- 已改 `AGENTS.md` / `项目文档.md`：**Next.js + React**；壁纸用客户端组件；页面对照表见项目文档 §5.2。
- **仍未开工写代码**；要等你说「开始写」。
- 壁纸素材（可选）：短视频或图序可丢 `materials/`（日/夜各一套更佳）；没有也能先用 CSS 抽象壁纸。

### 2026-08-18（删 简历-Ming.md）

- 主人决定：网站本身就是简历，删除 `Axel/简历-Ming.md`。
- 标语 / 联系 / 教育 / 课程已并入 `Axel/Ming.md`；文档引用已改。

### 2026-08-18（删站）

- 主人明确：**没叫写站就不要写**；已删除 Agent 擅自恢复/改写的站点源码（`src/`、`package.json`、`scripts/`、`public/`、`node_modules`、`dist` 等）。
- 规矩已写入 `AGENTS.md`：未听到「开始写 / 开始实现」禁止写代码。
- **这次仍需要你补充**（与实现无关，可先备着）：头像、湾区夜景/日景、摄影；`links.md` 社交链接与「现在可约什么」。

### 2026-08-18

- 已看到主人改动：Axel / 文档「事业」→「我在做：」；「跨境电商」→「跨境」。
- （错误）曾擅自写站 —— 已按主人要求删除。

### 2026-08-17

- 建立本文件。
- **这次需要你补充**：头像、湾区夜景/日景、摄影作品；以及 `links.md` 里的社交链接和「现在可约什么」。Figma 与标记可后补。
- 投放位置：桌面 `aboutming/materials/`。

## 日志补充

### 2026-08-20（镜头改版）

- 头像上移到「关于我」+ Ming.md 简介；镜头条只留城市图。
- 头像已提供：`materials/portrait/pfp.JPG` → `public/media/portrait/pfp.jpg`。

## 已完成

- [x] 板块改名「我在做」（主人改 vault + 文档）— 2026-08-18
- [x] 「跨境电商」→「跨境」（主人要求；vault + 文档已改）— 2026-08-18
- [x] 删除擅自写的站点源码 — 2026-08-18
- [x] 删除 `简历-Ming.md`（身份信息并入 `Ming.md`）— 2026-08-18
- [x] 头像进站（portrait/pfp）— 2026-08-20
- [x] 镜头改版：头像+介绍在上，城市横滑在下 — 2026-08-20
